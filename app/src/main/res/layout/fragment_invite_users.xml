<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_margin="@dimen/material_margin_default">

    <RelativeLayout
        android:id="@+id/session_invite_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <android.support.design.widget.TextInputLayout
            android:id="@+id/text_input_layout_email"
            style="@style/Material.TextInputLayout"
            app:errorTextAppearance="@style/Material.TextInputLayout.Error"
            app:hintTextAppearance="@style/Material.TextInputLayout.Hint"
            android:layout_centerInParent="true"
            android:layout_margin="@dimen/material_margin_default"
            >

            <EditText
                android:id="@+id/form_email"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:hint="@string/form_email_hint"
                android:inputType="textEmailAddress" />

        </android.support.design.widget.TextInputLayout>

        <Button
            android:id="@+id/btn_invite"
            style="@style/Material.Button.Raised"
            android:layout_width="match_parent"
            android:layout_height="72dp"
            android:layout_gravity="center"
            android:layout_marginBottom="@dimen/material_content_space"
            android:text="@string/action_join"
            android:textSize="20sp"
            android:layout_alignParentBottom="true"/>

    </RelativeLayout>

    <RelativeLayout
        android:id="@+id/session_load_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone">

        <ProgressBar
            android:id="@+id/join_progressbar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:indeterminate="false"
            />

        <TextView
            android:id="@+id/join_progresstext"
            android:layout_below="@+id/join_progressbar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/material_margin_default"
            tools:text="2 out of 6 people joined."
            android:gravity="center"
            android:text="@string/session_waiting_for_organizer_to_start_session"
            />

    </RelativeLayout>


</RelativeLayout>